export default class Categoty {

    constructor( {  name = null, desc = null, enable = null, slug = null } = {}) {
        this.name = name;
        this.desc = desc;
        this.enable = enable;
    }

}